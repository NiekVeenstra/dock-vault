Dock Vault text patch

This patch removes em/en dashes from visible website copy and rewrites the affected sentences so they still read naturally in Dutch and English.

Extract this ZIP into the root of your current dock-vault project and allow it to overwrite the matching files.

It intentionally does NOT contain package.json or package-lock.json, so your current Next.js security update will not be reverted.
