export function HarborDivider({ compact = false }: { compact?: boolean }) {
  return (
    <div className={`harbor-divider${compact ? ' harbor-divider--compact' : ''}`} aria-hidden="true">
      <span />
      <i />
      <span />
    </div>
  );
}
